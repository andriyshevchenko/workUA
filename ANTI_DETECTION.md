# 🕵️ Anti-Detection Features

Бот тепер використовує просунуті техніки обходу детекції ботів, щоб Work.ua думав що це реальна людина.

## 🛡️ Реалізовані техніки

### 1. **Playwright Stealth Mode**
- Автоматичне видалення `navigator.webdriver` property
- Приховування ознак automation
- Маскування Chrome runtime
- Використання бібліотеки `playwright-stealth`

### 2. **Реалістичні настройки браузера**
- Рандомні User-Agent з актуальними версіями Chrome
- Реалістичний viewport (1920x1080)
- Локаль та часова зона України (uk-UA, Europe/Kyiv)
- Геолокація Києва
- Правильні permissions та device scale factor

### 3. **Людиноподібна поведінка**

#### Рухи миші
- Плавні рухи з Bezier кривими
- Jitter (невеликі відхилення) як у людини
- Рандомні рухи по сторінці під час "читання"

#### Затримки
- **random_delay()**: 0.5-2.0 секунди між діями
- **typing_delay()**: 50-150ms між символами
- **reading_delay()**: Залежить від довжини тексту (200-300ms на слово)
- **page_load_delay()**: 1-3 секунди після завантаження
- Іноді довші паузи (як людина думає)

#### Прокручування
- Плавне прокручування з паузами
- 3-6 кроків прокручування
- Паузи після прокручування (людина читає)

#### Введення тексту
- Посимвольне введення з випадковими затримками
- Іноді довші паузи (як людина думає що писати)
- Пауза перед початком друку

### 4. **Інтеграція в всі дії**

Кожна дія бота тепер емулює людську поведінку:

- **Авторизація**: Рухи миші → пауза → введення тексту як людина → пауза → клік
- **Пошук вакансій**: Завантаження → рухи миші → введення → пауза → клік → прокручування
- **Перегляд вакансії**: Завантаження → прокручування → "читання" тексту → рухи миші
- **Подача заявки**: Прокручування → пауза (думає) → клік → пауза → клік

## 🔧 Технічні деталі

### Стек технологій
```python
playwright-stealth>=1.0.0  # Stealth mode
playwright>=1.41.0          # Browser automation
```

### Нові файли
- **human_behavior.py**: Клас `HumanBehavior` з усіма функціями емуляції

### Оновлені файли
- **scraper.py**: 
  - Імпорт `stealth_async` та `HumanBehavior`
  - Оновлений `start()` з реалістичними налаштуваннями
  - Всі методи використовують людиноподібну поведінку
- **pyproject.toml**: Додано `playwright-stealth`

## 📊 Що робить бот схожим на людину

### Детекція ботів шукає:
❌ Миттєві дії  
❌ Прямолінійний рух миші  
❌ Однакові затримки  
❌ `navigator.webdriver = true`  
❌ Відсутність руху миші  
❌ HeadlessChrome в User-Agent  

### Наш бот:
✅ Випадкові затримки (0.5-4 секунди)  
✅ Плавні рухи миші з Bezier  
✅ Прокручування як людина читає  
✅ `navigator.webdriver = undefined`  
✅ Випадкові рухи миші по сторінці  
✅ Реалістичний User-Agent  
✅ Імітація читання тексту  
✅ Паузи перед діями (людина думає)  

## 🎯 Приклад використання

```python
from human_behavior import HumanBehavior

# Клік з людиноподібною поведінкою
await HumanBehavior.click_with_human_behavior(
    page,
    'button[type="submit"]',
    scroll_into_view=True
)

# Введення тексту як людина
await HumanBehavior.type_like_human(
    page,
    'input[name="search"]',
    "python developer"
)

# Прокручування
await HumanBehavior.scroll_page_human_like(
    page,
    scroll_distance=500
)

# Рандомні рухи миші
await HumanBehavior.random_mouse_movement(page, num_movements=3)
```

## 🚀 Результат

Work.ua тепер сприймає бота як реальну людину завдяки:
1. Відсутності automation fingerprints
2. Реалістичній поведінці браузера
3. Людиноподібним паузам та рухам
4. Правильним метаданим (locale, timezone, geolocation)
5. Імітації читання та обмірковування

## ⚡ Performance

Бот працює повільніше (як людина), але це **features, not bugs**:
- 2-4 секунди між сторінками
- 1-3 секунди затримки після завантаження
- 0.5-1.5 секунди "читання" після прокручування
- 50-150ms між символами при введенні

Це забезпечує максимальну схожість на людину і мінімізує ризик блокування.
