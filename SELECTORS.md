# 🔍 Виявлені селектори Work.ua

## ✅ Авторизація (Login Page)

**URL:** `https://www.work.ua/jobseeker/login/`

**Елементи:**
- **Поле телефону:** `page.get_by_role('textbox')` - перший textbox на сторінці
- **Кнопка входу:** `page.get_by_role('button', name='Увійти')`
- **Посилання "Увійти":** `page.get_by_role('link', name='Увійти')` - на головній сторінці

## 🔎 Пошук вакансій (Search Page)

**URL:** `https://www.work.ua/jobs/`

**URL для дистанційної роботи:** `https://www.work.ua/jobs/remote/`

**Форма пошуку:**
- **Поле пошуку:** `page.get_by_role('textbox', name='Посада або компанія')`
- **Поле місто:** `page.get_by_role('textbox', name='Місто')`
- **Кнопка пошуку:** `page.get_by_role('button', name='Знайти вакансії')`

**Дистанційна робота (Remote):**
- **Посилання:** `page.get_by_role('link', name='Дистанційна робота')` → `/jobs/remote/`
- **URL pattern після пошуку:** `/jobs-remote-<keywords>/?advs=1`
- **Приклад:** пошук "python developer" remote → `/jobs-remote-python+developer/?advs=1`
- **Поле місто автоматично:** "Дистанційно" (не треба заповнювати)

**Результати пошуку:**
- **Заголовки вакансій:** `page.get_by_role('heading', level=2)` - всі h2 це вакансії
- **Посилання вакансії:** всередині h2 є `<a>` з `href="/jobs/XXXXXX/"`

**Структура вакансії в списку:**
```yaml
- heading [level=2]:
  - link: назва вакансії + url
- generic: компанія + локація
- paragraph: короткий опис
- time: дата публікації
- button "Не показувати"
- link "Зберегти"
```

## 📄 Сторінка вакансії (Job Details)

**URL:** `https://www.work.ua/jobs/XXXXXX/`

**Елементи:**
- **Назва вакансії:** `page.get_by_role('heading', level=1)` - "Python developer (Django, FastAPI)"
- **Кнопка відгуку (2 екземпляри):** 
  - `page.get_by_role('button', name='Відгукнутися')` - вгорі сторінки
  - `page.get_by_role('button', name='Відгукнутися')` - внизу сторінки
  - Використовуємо `.first` щоб взяти першу (верхню)
- **Опис вакансії:** 
  - Заголовок: `page.get_by_role('heading', level=2, name='Опис вакансії')`
  - Контент: наступний sibling після заголовка
- **Компанія:** посилання з назвою компанії
- **Зарплата:** у listitem з generic "Зарплата"
- **Локація:** у listitem з generic "Місце роботи"
- **Умови:** у listitem з generic "Умови й вимоги"
- **Знання мов:** у listitem з generic "Знання мов"

**Додаткові кнопки:**
- **Зберегти:** `page.get_by_role('link', name='Зберегти')`
- **Ще (додаткові опції):** `page.get_by_role('button', name='Ще')`

**Навігація:**
- **Результати пошуку:** `page.get_by_role('link', name='Результати пошуку')` - повертає до списку

## 📝 Форма відгуку (Apply Dialog)

**Workflow відгуку:**
1. Клік на `button "Відгукнутися"` → відкривається dialog
2. У dialog автоматично вибране збережене резюме
3. Клік на `button "Надіслати"` → відгук надсилається
4. Може з'явитися додатковий dialog про додавання локації
5. Клік на `button "Не додавати"` або `button "Додати дистанційну роботу"`
6. Перенаправлення на `/jobseeker/my/resumes/sent/?job_id=XXXX`

**Елементи форми:**
- **Dialog:** активний діалог з формою відгуку
- **Заголовок:** `heading level=1 "Відгукнутися"`
- **Таби:**
  - "Вибрати збережене резюме" (за замовчуванням)
  - "Завантажити резюме"
- **Чекбокси:**
  - "Додати супровідний текст"
  - "Додати посаду до резюме"
  - "Надсилати схожі вакансії"
- **Кнопки:**
  - `button "Надіслати"` - відправка відгуку
  - `button "Скасувати"` - закриття форми
  
**Додатковий dialog (після надсилання):**
- **Заголовок:** `heading level=2 "Додайте дистанційну роботу до резюме"`
- **Кнопки:**
  - `button "Не додавати"` - пропустити
  - `button "Додати дистанційну роботу"` - додати локацію

**Перевірка успіху:**
- URL містить `/sent/` → відгук успішний
- Сторінка "Відгук надісланий" з заголовком "Чудово! Залишилось ще X відгуки!"

## 🗺️ Навігація

**Головне меню:**
- **Знайти вакансії:** `page.get_by_role('link', name='Знайти вакансії')`
- **Рекомендовані вакансії:** `page.get_by_role('link', name='Рекомендовані вакансії')`
- **Мій розділ:** `page.get_by_role('link', name='Мій розділ')`
- **Сповіщення:** `page.get_by_role('button', name='Сповіщення')`

## 💡 Важливі notes

### Role-based селектори (рекомендовано):
✅ `page.get_by_role('button', name='...')` - для кнопок
✅ `page.get_by_role('link', name='...')` - для посилань
✅ `page.get_by_role('textbox', name='...')` - для полів вводу
✅ `page.get_by_role('heading', level=X)` - для заголовків

### CSS селектори (fallback):
- Вакансії мають різні класи, тому краще use headings
- Компанія часто в `[class*="company"]` або `[class*="employer"]`
- Зарплата в тексті формату "X грн"

### Pagination:
- Перевірка наступної сторінки: шукати `<a rel="next">`
- URL pattern: `?page=2`, `?page=3` тощо

### Особливості:
1. Work.ua використовує React (React build v1.0.1171)
2. Структура може динамічно змінюватись
3. Role-based селектори найстабільніші
4. Після авторизації cookies зберігаються в `cookies.json`
5. SMS код треба вводити вручну (чекаємо 120 сек)
