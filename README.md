# 🤖 Work.ua Бот

Автоматичний бот для розсилки резюме на вакансії Work.ua з опціональним LLM аналізом та **продвинутим обходом детекції ботів**.

## ✨ Особливості

- 🎯 **Автоматична розсилка** - сканує вакансії та відгукується автоматично
- 🤖 **Опціональний LLM аналіз** - GPT-4o фільтрує вакансії за відповідністю
- 🕵️ **Stealth режим** - обходить детекцію ботів, поводиться як людина
- 🖱️ **Людиноподібна поведінка** - рухи миші, затримки, прокручування
- 🌐 **Playwright автоматизація** - надійний веб-скрапінг
- 📊 **Детальне логування** - в консоль та файл
- 🔐 **Безпечна авторизація** - через cookies
- 🎛️ **Конфігурабельно** - всі налаштування в .env

## 🛡️ Anti-Detection

Бот використовує просунуті техніки обходу детекції:

- ✅ **Playwright Stealth** - видаляє automation fingerprints
- ✅ **Реалістичні рухи миші** - з Bezier кривими та jitter
- ✅ **Випадкові затримки** - як людина думає та читає
- ✅ **Прокручування сторінок** - імітація читання
- ✅ **Realistic browser context** - user-agent, viewport, locale, timezone
- ✅ **Посимвольне введення** - з випадковими паузами

📖 Детальніше: [ANTI_DETECTION.md](ANTI_DETECTION.md)

## 📋 Вимоги

- Python 3.11+
- [uv](https://github.com/astral-sh/uv) - швидкий пакетний менеджер

## 🚀 Швидкий старт

### 1. Встановіть uv

**Windows (PowerShell):**
```powershell
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

### 2. Встановіть залежності

```bash
cd workUA
uv sync
```

### 3. Налаштуйте .env

```bash
cp .env.example .env
```

Відредагуйте `.env`:

```env
# API ключ (опціонально - для LLM аналізу)
OPENAI_API_KEY=sk-your-key-here

# Work.ua телефон для SMS авторизації
WORKUA_PHONE=+380XXXXXXXXX

# Пошук
SEARCH_KEYWORDS=python developer,backend developer
LOCATIONS=Київ,Львів
REMOTE_ONLY=false

# Налаштування бота
MAX_APPLICATIONS=10     # Скільки вакансій відгукнутись
USE_LLM=false           # true = LLM фільтрує, false = брут форс
MIN_SCORE=7             # Мінімальний score для відгуку (якщо LLM увімкнено)
HEADLESS=false          # true = невидимий браузер

# Модель (якщо USE_LLM=true)
MODEL_NAME=gpt-4o
```

### 4. Встановіть Playwright

```bash
uv run playwright install chromium
```

### 5. Авторизуйтесь на Work.ua

```bash
uv run python explorer.py
```

Введіть SMS код коли браузер відкриється.

### 6. Запустіть бот!

```bash
uv run python bot.py
```

## 📁 Структура проекту

```
workUA/
├── bot.py              # 🤖 Головний бот
├── scraper.py          # 🌐 Web scraping з Playwright
├── explorer.py         # 🔍 Дослідження структури Work.ua
├── config.py           # ⚙️ Конфігурація
├── pyproject.toml      # 📦 Залежності
├── .env                # 🔐 Ваші налаштування
├── logs/               # 📝 Логи роботи бота
└── cookies.json        # 🍪 Сесія авторизації
```

## 🎯 Режими роботи

### 1. Брут форс (без LLM)
```env
USE_LLM=false
```
- Відгукується на **всі** знайдені вакансії
- Швидко
- Не потребує API ключа

### 2. З LLM аналізом
```env
USE_LLM=true
OPENAI_API_KEY=sk-...
MIN_SCORE=7
```
- GPT-4o аналізує кожну вакансію
- Порівнює з вашим резюме
- Відгукується тільки якщо score >= MIN_SCORE
- Економить час на нерелевантних вакансіях

## 📊 Логування

Бот створює детальні логи:
- **Консоль:** Основні події (INFO)
- **Файл:** Повна інформація (DEBUG)
- **Папка:** `logs/workua_bot_YYYYMMDD_HHMMSS.log`

Приклад логів:
```
2026-02-01 15:30:00 | INFO     | 🤖 WORK.UA BOT - Автоматичний пошук роботи
2026-02-01 15:30:05 | INFO     | ✅ Авторизація успішна
2026-02-01 15:30:05 | INFO     | 🔍 Ключові слова: python developer
2026-02-01 15:30:05 | INFO     | 🎯 Мета: 10 відгуків
2026-02-01 15:30:10 | INFO     | 📌 Python Developer (Django)
2026-02-01 15:30:10 | INFO     | 🏢 UncleSolutions
2026-02-01 15:30:12 | INFO     | 🤖 LLM оцінка: 8/10
2026-02-01 15:30:15 | INFO     | ✅ Відгукнулись! (1/10)
```

## 🛠️ Команди uv

```bash
# Встановити залежності
uv sync

# Запустити бот
uv run python bot.py

# Авторизація
uv run python explorer.py

# Тести
uv run python test_remote.py
uv run python test_apply.py
```

## 🔧 Налаштування бота

### Резюме
Відредагуйте метод `_load_resume()` в [bot.py](c:\Users\Ernest\Desktop\workUA\bot.py) або додайте файл резюме:

```python
def _load_resume(self) -> str:
    resume_path = Path(config.RESUME_PATH)
    if resume_path.exists():
        return resume_path.read_text(encoding='utf-8')
    return "Ваше резюме тут..."
```

### Ключові слова
```env
SEARCH_KEYWORDS=python developer,backend developer,django developer
```

### Кількість відгуків
```env
MAX_APPLICATIONS=20  # Відгукнеться на 20 вакансій
```

### LLM поріг
```env
MIN_SCORE=8  # Відгукуватись тільки якщо вакансія дуже підходить
```
    F --> G{Підходить?}
    G -->|Так| H[Відгук на вакансію]
    G -->|Ні| I[Пропустити]
    H --> J[Наступна вакансія]
    I --> J
    J --> D
    D -->|Ні| K[Звіт та завершення]
```

## ⚡ Швидке тестування

```bash
# Дослідити сайт (з відкритим браузером)
uv run python explorer.py

# Запустити бота
uv run python bot.py
```

## 🐛 Налагодження

Якщо виникають проблеми:

1. **Помилка авторизації**: Запустіть `explorer.py` та авторизуйтесь вручну
2. **Браузер не запускається**: `uv run playwright install chromium`
3. **API помилки**: Перевірте ваші API ключі в `.env`

## 📄 Ліцензія

MIT

## 🤝 Внесок

Pull requests вітаються! Для великих змін спочатку відкрийте issue.

---

**Зроблено з ❤️ використовуючи LangGraph + Playwright**
